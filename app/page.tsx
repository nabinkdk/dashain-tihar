// Make sure this path matches exactly what you named the file in your components folder!
// If you named it DashainApp.tsx, change it here too.
import FestivalApp from "@/components/DashainApp";

export default function Home() {
  return <FestivalApp />;
}
